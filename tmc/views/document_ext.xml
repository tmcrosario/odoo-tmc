<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- WINDOW ACTION -->
    <act_window id="document_ext_list_action" name="Resoluciones Extraordinarias" res_model="tmc.document" view_type="form" view_mode="tree,form" domain="[('document_type_id','=','Resolución Extraordinaria')]" />

    <!-- TREE VIEW -->
    <record id="view_document_ext_tree" model="ir.ui.view">
        <field name="name">tmc.document_ext.tree</field>
        <field name="model">tmc.document_ext</field>
        <field name="arch" type="xml">
            <tree>
                <field name="document_id" />
            </tree>
        </field>
    </record>

    <!-- FORM VIEW -->
    <record id="view_document_ext_form" model="ir.ui.view">
        <field name="name">tmc.document_ext.form</field>
        <field name="model">tmc.document_ext</field>
        <field name="inherit_id" ref="tmc.view_document_form" />
        <field name="priority">5</field>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="document_id" invisible="True" />
            </xpath>
            <!-- <button name="show_or_add_content" position="attributes">
                <attribute name="invisible">True</attribute>
            </button> -->
        </field>
    </record>

</odoo>
